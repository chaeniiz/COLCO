<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chaeniiz.colco">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.FeedActivity" />
        <activity
            android:name=".ui.activity.LoginActivity"
            android:theme="@style/Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.RegisterActivity" />
        <activity android:name=".ui.activity.ScrapActivity" />
        <activity android:name=".ui.activity.VoteActivity" />
        <activity android:name=".ui.activity.VoteDetailActivity" />
        <activity android:name=".ui.activity.MyPageActivity" />
        <activity android:name=".ui.activity.SettingActivity" />
        <activity
            android:name=".ui.activity.UploadActivity"
            android:theme="@style/Transparent" />
        <activity android:name=".ui.activity.UploadFeedActivity" />
        <activity android:name=".ui.activity.UploadItemActivity"></activity>
    </application>

</manifest>