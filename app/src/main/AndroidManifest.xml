<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chaeniiz.colco">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FeedActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ScrapActivity" />
        <activity android:name=".VoteActivity" />
        <activity android:name=".VoteDetailActivity" />
        <activity android:name=".MyPageActivity" />
        <activity android:name=".SettingActivity" />
        <activity
            android:name=".UploadActivity"
            android:theme="@style/Transparent" />
        <activity android:name=".UploadFeedActivity" />
        <activity android:name=".UploadItemActivity"></activity>
    </application>

</manifest>